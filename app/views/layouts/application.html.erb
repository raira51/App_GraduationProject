<!DOCTYPE html>
<html>
  <head>
    <title>Letteria</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag "favicon.ico" %>
    <%= favicon_link_tag "favicon.ico", rel: "apple-touch-icon" %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="min-h-screen w-full bg-stone-50 <%= current_page?(root_path) ? 'overflow-hidden' : 'overflow-auto' %>">
    <% if logged_in? %>
      <header class="sticky top-0 z-50 border-b bg-white/80 backdrop-blur">
        <div class="mx-auto flex max-w-4xl items-center justify-between px-4 py-3">
          <%= link_to "Letteria", root_path, class: "text-lg font-semibold" %>

          <nav class="flex items-center gap-4 text-sm">

            <%= button_to "ログアウト", logout_path, method: :delete, class: "bg-transparent p-0 hover:underline" %>
          </nav>
        </div>
      </header>
    <% end %>

    <%= yield %>
  </body>
</html>

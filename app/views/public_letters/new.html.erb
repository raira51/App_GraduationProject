<div class="min-h-screen w-full overflow-auto bg-stone-50">
  <div class="mx-auto max-w-5xl px-4 py-10">
    <div class="mx-auto max-w-md">

      <div class="rounded-2xl border border-stone-200 bg-white p-8 shadow-sm">

        <div class="mb-6 text-center">
          <h1 class="text-2xl font-bold tracking-tight">メッセージ投稿</h1>
          <p class="mt-2 text-sm text-stone-600">
            <span class="font-semibold text-stone-700"><%= @celebration.receiver_name %></span>さんへの
            お祝いメッセージを残せます。
          </p>
        </div>
        
        <% if flash[:notice] %>
          <div class="mb-4 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700">
            <%= flash[:notice] %>
          </div>
        <% end %>

        <% if flash[:alert] %>
          <div class="mb-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">
            <ul class="list-disc pl-5 space-y-1">
              <% Array(flash[:alert]).each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <%= form_with model: [@celebration, @letter], url: "/l/#{@celebration.share_url}", class: "space-y-5", data: { turbo: false } do |f| %>

          <div>
            <%= f.label :sender_name, "送信者名（ニックネーム）*",
                  class: "block text-sm font-medium text-stone-700" %>
            <%= f.text_field :sender_name,
                  class: "mt-1 w-full rounded-xl border border-stone-300 px-4 py-3",
                  placeholder: "例：らんてくん" %>
          </div>

          <div>
            <%= f.label :body, "メッセージ*",
                  class: "block mt-4 text-sm font-medium text-stone-700" %>
            <%= f.text_area :body,
                  rows: 6,
                  class: "mt-1 w-full rounded-xl border border-stone-300 px-4 py-3",
                  placeholder: "例：卒業おめでとう！これからも仲良くしてね☺" %>

            <p class="mt-2 text-center text-sm text-stone-500">
              投稿内容はお祝いルームの作成者も閲覧できます。
            </p>
          </div>

          <div class="mt-8 mb-2 flex justify-center">
            <%= f.submit "送信する",
                  class: "block w-full rounded-xl bg-teal-700 px-6 py-3 text-center text-sm font-semibold text-white shadow-[0_4px_0_#115e59] transition-all duration-150 hover:bg-teal-600 active:translate-y-[3px] active:scale-[0.98] active:shadow-none" %>
          </div>

        <% end %>

        <div class="mt-6 border-t border-stone-200 pt-5">
          <p class="mt-4 text-xs text-center text-stone-500">
            このページのURLを共有すると、複数人がメッセージを送れます。
          </p>

          <div class="mt-3 rounded-xl border border-stone-200 bg-stone-50 p-3 text-xs text-stone-700 break-all">
            <%= public_letter_new_url(@celebration.share_url) %>
          </div>
        </div>

      </div>
    </div>
  </div>
</div>
